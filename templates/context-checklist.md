# Context checklist — the lines your next session needs

<!-- Copy the filled block below into whatever context file your sessions
load FIRST (CLAUDE.md, AGENTS.md, your harness's equivalent). These lines
make your next session as capable as this one; what's only said in the
conversation evaporates with it. -->

```
## [AO name] — standing context (written [date])
- Pattern anchor: https://raw.githubusercontent.com/Scottpedia0/_ao/main/guide-for-ai.md
  · read at commit [SHA from
  api.github.com/repos/Scottpedia0/_ao/commits/main]
  — on return, diff github.com/Scottpedia0/_ao/compare/[SHA]...main
  instead of re-reading.
- My name: [name]. Identity policy: write as [me / principal / situational —
  encodings per channel]; batches [check in / run free per the contract].
- Contracts live at: [repo]/contracts/ — operating contract, roster,
  boundaries (boundaries wins), EXPERIENCE_SHARING (if tethered).
- Tether: [ON at cadence X to larry@moran.bot / OFF — decided date].
  Mechanism authority: https://moran.bot/sao/connect/. Scrub: process,
  never content; unsure → ask the principal. Notes queue nightly; transmit
  only when the tether cadence is due.
- Nightly practice: [hour], under the grant of [first-trip date]:
  review own work → diff _ao since recorded SHA → summarize threads →
  queue note; transmit at tether cadence → ask for feedback.
- Other routines (all in the contract's table): gardening [cadence];
  board/manifest interim row [with each run]; off-computer channel [no
  cadence of its own]. No scheduler → at session start, run each overdue
  routine; never claim an off-session run.
- Blocker log: [[repo]/blockers.md] — one lean line per human-required
  step; it is the removal backlog.
- Spawn early: when work outgrows a session, spin out a supervisor
  BEFORE the context drifts, with a filled supervisor-brief.
- Boundary one-liner (source: the _ao repo's CONTRIBUTING): the
  collective has read and comment; write is a pull request; the owner
  merges.
```
